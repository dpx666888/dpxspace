# 第014次开发记录

## 开发时间

2026-07-22

## 本次目标

完成阶段9.6：安全加固与部署配置。验证并修复 RLS 策略，添加 Vercel 部署配置，生成部署说明文档。

## 修改文件

新增：

- `supabase/migrations/002_fix_rls_policies.sql`
- `vercel.json`
- `docs/deployment/VERCEL_DEPLOY.md`
- `docs/development-log/014-阶段9.6-安全加固与部署配置.md`

修改：

- `supabase/migrations/001_initial_schema.sql`：将 RLS 策略从 `auth.role() = 'authenticated'` 改为 `auth.uid() IS NOT NULL`，避免未来新部署出现权限绕过

## 完成内容

1. 编写并测试新的 RLS 修复迁移脚本
2. 更新初始迁移脚本中的 RLS 策略，使用更安全的 `auth.uid() IS NOT NULL`
3. 添加 `vercel.json` 配置 SPA 路由重写，支持 React Router 刷新页面
4. 编写 `docs/deployment/VERCEL_DEPLOY.md` 部署指南
5. `npm run build` 通过

## 遇到的问题

1. 使用 Node 脚本测试时发现匿名 key 可以成功执行 `update/delete/insert`，RLS 没有生效

## 解决方案

1. 将 RLS 策略条件从 `auth.role() = 'authenticated'` 改为 `auth.uid() IS NOT NULL`
2. 新增 `002_fix_rls_policies.sql` 用于已执行过旧迁移的现有数据库

## 当前状态

完成

## 下一步计划

1. 在 Supabase SQL Editor 中执行 `supabase/migrations/002_fix_rls_policies.sql`
2. 执行后重新用 Node 脚本验证匿名写入被阻止
3. 将代码推送到 GitHub
4. 在 Vercel 导入项目并配置环境变量 `VITE_SUPABASE_URL`、`VITE_SUPABASE_ANON_KEY`
5. 部署完成后访问前台和后台测试全流程

## 备注

- 管理员账号 `a13265405786@outlook.com` / `dpx2060786339` 已通过 Node 脚本验证登录成功
- `.env.local` 不上传 Git，Vercel 环境变量需单独配置
- 占位内容继续保留占位符，未提前填充
